# permutation problem
def permu(n):
	list = [] 
	options = []
	for x in xrange(0, n+1):
		options.append(x)
	for attempt in xrange(0, 1000):
		suboptions = options
	# finds the permuations from 0 to n, returns list
	return list

print permu(2)
